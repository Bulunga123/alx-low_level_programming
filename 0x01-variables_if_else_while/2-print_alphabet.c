#include <stdio.h>
/**
 * main - print function
 * Return: return 0 for now
 */
int main(void)
{
	char alphabets[26] = "abcdefghijklmnopqrstuvwxyz";
	int i;

	for (int i = 0, i < 26, i++)
	{putchar(alphabets[i];	}
			putchar("\n");
			return (0);
}
